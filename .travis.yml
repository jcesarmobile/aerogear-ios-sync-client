language: objective-c
osx_image: xcode8.1
xcode_workspace: AeroGearSyncClientJsonPatch.xcworkspace
xcode_scheme: AeroGearSyncClientJsonPatch
xcode_sdk: iphonesimulator

before_install:
  - pod repo update

notifications:
  irc: "irc.freenode.org#aerogear"

branches:
  only:
    - master
script:
  - xcodebuild -workspace AeroGearSyncClientJsonPatch.xcworkspace -scheme AeroGearSyncClientJsonPatch -sdk iphonesimulator -destination 'platform=iOS Simulator,name=iPhone 7,OS=10.1' build